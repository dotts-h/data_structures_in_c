# Lab 7 - Binary Search

## Operatii Principale - Main Operations

> Sa se afiseze un meniu ce contine urmatoarele optiuni
> Show the following menu

- Manually generate the vector (read n as size of the vector)
- Automatic generation of the vector (red n as size of the vector)
- Print
- Search

> Vectorul va contine numere intregi, iar ordonarea va fi realizata la inserare
> The vector uses integers and sorting happens at creation

## Operatii Extinse - Extra Operation

> Scopul operatiilor extinse este de a demonstra utilitatea cautarii binari prin comparare cu cautarea secventiala. Se va utiliza un vector uzual in care elementele sunt adaugate secvential si un vector in care elementele sunt inserate astfel incat sa rezulte un vector sortat. Sa se afiseze un meniu cu urmatoarele optiuni
> The purpose of these operation is to prove the utility of binary search compared to sequential search. This uses a normal vector where elements are added sequentially and another vector where elements are added so that the vector is sorted. Show the following menu

- Automatic generation of the two vectors (read from input the size)
- Print elements on position n/4, n/2 and n
- Search time of element on position n/4 (sorted vector)
- Search time of element on position n/2 (sorted vector)
- Search time of element on position n (sorted vector)

> Timpul de cautare va fi masurat pentru n=100, n=10000 si n=100000
> Search time will be checked for n=100, n=10000 and n=100000
